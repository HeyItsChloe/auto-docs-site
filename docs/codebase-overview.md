# Codebase Overview

*Not generated yet.* This page is rewritten automatically by
[`scripts/generate-overview.mjs`](https://github.com/HeyItsChloe/auto-docs-site/blob/main/scripts/generate-overview.mjs)
the first time the workflow runs — either via `workflow_dispatch`, a push to `main`, or the
weekly schedule, whichever is configured as active.

Run it locally to generate it now:

```sh
npm install
npm run generate:overview -- --engine=deterministic
# or, with ANTHROPIC_API_KEY set:
npm run generate:overview -- --engine=llm
```
